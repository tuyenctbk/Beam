package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Minimalism Monochromatic Slate Palette
val BeamPrimary = Color(0xFF0F172A)          // Slate 900
val BeamOnPrimary = Color(0xFFFFFFFF)        // White
val BeamPrimaryContainer = Color(0xFFF1F5F9) // Slate 100
val BeamOnPrimaryContainer = Color(0xFF0F172A) // Slate 900

val BeamSecondary = Color(0xFF64748B)        // Slate 500
val BeamSecondaryContainer = Color(0xFFE2E8F0) // Slate 200
val BeamOnSecondaryContainer = Color(0xFF1E293B) // Slate 800

val BeamBackground = Color(0xFFF8FAFC)       // Slate 50
val BeamOnBackground = Color(0xFF0F172A)     // Slate 900

val BeamSurface = Color(0xFFFFFFFF)          // Crisp White
val BeamOnSurface = Color(0xFF0F172A)        // Slate 900

val BeamOutline = Color(0xFFE2E8F0)          // Slate 200
val BeamAccent = Color(0xFF2563EB)           // Refined Blue Accent

val BeamSuccess = Color(0xFF10B981)          // Emerald 500
val BeamWarning = Color(0xFFD97706)          // Amber 600
val BeamWarningBg = Color(0xFFFEF3C7)        // Amber 100
val BeamError = Color(0xFFDC2626)            // Red 600
val BeamErrorBg = Color(0xFFFEE2E2)          // Red 100
